DATABASE_URL=postgres://ogindexer:ogindexer@localhost:5432/ogindexer

# Yellowstone Geyser gRPC endpoint the indexer connects to.
# Local test-validator + plugin listens on 127.0.0.1:10000 (see PROJECT.md §9).
GRPC_URL=http://127.0.0.1:10000
# Auth header for hosted providers; leave unset for local.
# GRPC_TOKEN=
